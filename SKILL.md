---
name: immersive-scroll
description: 把静态资料（PPT/PPTX、Word/DOCX、PDF、图片目录）编排成单文件沉浸式 HTML 滚动页（可离线、双击即用、微信可直接转发；滚动叙事、章节导航、图片懒注入、点击放大、手机端自适应；可选"乙档漫游"增强：GSAP 钉住擦洗、构造剖面穿越、惯性滚动——横向劫持滚轮属可用性事故默认不做）。覆盖探针评估、图文提取、分级压缩、叙事重构、构建、静态验证、双视口真机取证流水线、**机制层防复发判据（判据分结构/几何/语义三层、修改引入回归、取证链路自证）**与全部已知坑。当用户要求"把 PPT/资料做成网页/宣传页/H5/沉浸式展示/可分享的单文件""PPT 转 HTML""宣传手册电子化""加滚动动画/漫游效果"时使用。
version: 2.5.0
agent_created: true
---

> **v2.5.0（2026-09-19 样式层拆分）**：`BASE_CSS` 拆为 **CORE_CSS（机制层，恒注入）** 与 **PATTERN_CSS（版式层，默认注入，`--no-pattern` 可关）**——此前 97 个选择器里 66 个是项目专属版式，焊在不可关闭的基础层里，第二个使用者会撞类名。验证用三重证据：computed style 快照前后 diff（144 元素 × 57 属性 × 双视口 = **零差异**，专防拆分后级联顺序反转）、BODY 逐字节相等、负向测试 8 例全有效。版式层关闭后通用判据仍全 PASS（真可关，不是名义可关）。
> **v2.4.1（2026-09-19 判据修复）**：建仓前做**判据负向测试**（注入已知缺陷，验证取证器是否真的会 FAIL），8 例中 3 例失效，已修：① 引用数判据只比数量不比成员，图 key 写错时 5===5 仍全绿、该图永远空白 → 改比**差集**；② 验收表承诺的几何级变形率判据脚本里**根本没有** → 补上，且只在 `object-fit` 为 fill/none 时判（contain 的定值画框盒子比天然≠素材比，照字面判会误报）；③ 禁用 JS 只查 `innerText.length`，`innerText` 不排除 `opacity:0`，全页透明仍 PASS → 加**可见性**断言。另补两条判据：导航锚点有对应章节、点击放大取主体图 `img.fg`（坑 16 落地）。

> **v2.4.0（2026-09-19 脚本资产）**：补上可执行资产，让使用者能跑而不只是读——`scripts/extract_pptx.py`（探针/提取/压缩）、`scripts/build_single_html.py`（机制层 + 组装 + 静态自检）、`scripts/verify_single_html.js`（双视口取证，项目专属判据改为命令行参数）、`examples/minimal/`（现场绘制的样例 PPTX + 示例编排 + 四步跑通说明）。七段流水线各段标注对应脚本；**修正压缩分级（小图长边 900 → 实测 1100）**并补全阈值与"只缩不放"；补页脚角标的可执行判定规则。
> **v2.3.0（2026-09-19 开源包装）**：补 LICENSE（MIT，含 GSAP/Lenis 第三方许可声明）与面向人阅读的 README；环境段拆为「跨平台通用」与「平台专有注记（WorkBuddy）」两段——后者对其他平台是噪音，一并隔离。
> **v2.2.0（2026-09-19 复盘整改）**：新增「机制层判据」一章（M1–M7），来自本次 4 轮用户反馈的根因复盘——实例层坑清单拦不住"修 A 引入 B""绿灯冒充有效""取证链路失真误诊"三类复发；验收判据表补语义级行；新增坑 14/15。
> **v2.1.0（2026-09-19）**：① 横移长卷降级为默认不做（坑 11：横向劫持滚轮=方向错配，用户实测无法浏览）；② 新增"定值画框"机制入正文方法论（坑 12：素材比例失配是常态——aspect-ratio 统一画框 + contain + 同图模糊底 + 几何平均比例）；③ 新增坑 13（取证黑屏/平滑滚动干扰）。实测载体：63 图 / 7.4 MB 单文件宣传手册（行业资料，非特定项目）。
> **v2.0.0（2026-09-19）**：① 技能通用化——身份从「PPT→宣传手册」改为「静态资料→可离线分享的单文件沉浸式滚动页」，输入源参数化（pptx 为 T1 已实测；docx/pdf/图片目录为 T2 判据，未实测，不写成已验证流水线）；② 清除本机路径硬编码，改为跨平台探测判据；③ 新增"漫游档位"（甲/乙/丙）与漫游四层增强；④ 回填乙档实测坑 8–10（body overflow-x 毒杀 sticky、平滑滚动库干扰取证、横向轨道裁剪）。
> v1.1.0：编排段补同构页识别法与章节自检判据；新增"必须交用户复核的三类事项"。
> v1.0.0：初版，七段流水线 + 7 个实测坑 + 本机环境事实。

# 静态资料 → 单文件沉浸式滚动页

## 定位

**这不是格式转换，是叙事重构。**
PPT/文档是分页的、绝对定位的；网页是滚动的、响应式的。像素级还原必然在手机和投屏上崩——交付目标是**重建信息层级**，不是复刻版式。开工前必须与用户对齐。

交付形态默认**单文件自包含**：图片 base64 内联、库内联（不得 CDN），双击即用、可离线、能直接微信发出去。
（可选 HTML + assets 目录，用于图片 >30 MB 需要懒加载的场景。）

## 输入源（按类型选探针与提取器）

| 源 | 状态 | 探针/提取 | 备注 |
|---|---|---|---|
| **PPT/PPTX** | **T1 已实测** | python-pptx + Pillow | 见下方七段流水线，全部判据源自实测 |
| DOCX | T2 未验证 | python-docx 读段落/表格/内嵌图 | 坑：正文图片在 `word/media`，需按出现顺序对齐段落 |
| PDF | T2 未验证 | PyMuPDF 逐页出图 + 抽文本 | 扫描件无文本层，需 OCR，工作量另议 |
| 图片目录 | T2 未验证 | 直接 glob + 文件名/EXIF 排序 | 无文本时章节全靠用户口述，先问再动 |

**T2 纪律**：未实测的源只写判据，不写"已验证步骤"。第一次跑通任一 T2 源后，把实测命令与坑回填本表并升 T1。

## 漫游档位（开工前与用户定档）

| 档 | 内容 | 代价 | 适用 |
|---|---|---|---|
| **甲·静态滚动叙事** | 淡入 + 章节导航 + lightbox | 零依赖，纯手写 JS | 通用默认 |
| **乙·漫游** | 甲 + GSAP/ScrollTrigger/Lenis 全内联（约 133 KB）；漫游四层：逐卡淡入、连续运镜、**钉住擦洗**（pin+scrub，仅纵向）、语义化行进路线；**横移劫持默认不做**（坑 11） | +133 KB；移动端必须降级 | 有"穿越/序列入场"叙事诉求 |
| **丙·WebGL/Three.js** | 3D 场景 | 体积大、低端机崩、维护重 | **已否决，不再提** |

乙档硬约束：单文件库内联；**移动端（≤960px）钉住/擦洗一律降级为普通滚动**（微信内置浏览器对 sticky/100vh 不稳定，长钉住段手机体验差是业界共识）；`prefers-reduced-motion` 时整层不启用。

---

## 七段流水线（以 PPTX 为例，其余源替换第 1–2 段）

**对应脚本**：第 1–3 段 `scripts/extract_pptx.py` · 第 4 段是人的判断（写在你的 sections 模块里，照 `examples/minimal/sections.py` 改）· 第 5–6 段 `scripts/build_single_html.py` · 第 7 段 `scripts/verify_single_html.js`。四步跑法见 `examples/minimal/README.md`。

**分工原则**：脚本只做**机制**（提取、压缩、内联、画框、取证），**编排不代做**——按内容语义归章不是页码等分，判据是人的判断，自动化只会退回第 4 段明令禁止的做法。

### 1. 探针（先摸清，别先动手）
用 python-pptx + Pillow 回读：**页数 / 图片数 / 表格图表数 / 素材总体积 / 最大单图 / 是否用占位符标题**。
三项决定方案可行性：
- **0 表格 0 图表 0 OLE**：纯图文，转换无障碍；有图表需另议（矢量重建或截图）。
- **无占位符标题**（全是自由文本框）：抽取只会得到无层级散文本 → 必须人工归类叙事章节，这是工作量主体。
- **素材体积**：>100 MB 必须先压缩，否则内联 base64 产出的 200 MB+ 单文件浏览器打不开。

命令：`python scripts/extract_pptx.py <in.pptx> <out_dir>` —— 一次跑完第 1–3 段，探针结论写入 `<out_dir>/probe_report.txt`（含判读提示）。

### 2. 提取（页-图-文映射）
输出到中间目录：`content.json`（按页的段落原文 + 每图的框位/面积/主次序）、`manifest.json`（media → webp 文件、字节、出现页、最大面积）、`media_webp/`（压缩产物）；原始素材只在中间态存在。
**版面面积是判断图片主次的唯一可靠依据**：占 0.7% 且出现在 25/27 页的必是页脚角标，直接排除——可执行判定规则「出现页数 ≥ 总页数 ×50% 且最大面积 <1%」，提取脚本自动执行（阈值可调，见 `--footer-page-ratio` / `--footer-max-area`）。
组合（group）内的图片要递归取出，并按 group 的仿射变换把子坐标换算到页面坐标，否则面积恒为 0、主次判据失效。
**文案一律按页索引取原文，不手抄、不改写**；导航性 UI 短语（章节标题、步骤标签）允许重构，但术语必须忠于原文。

### 3. 压缩（决定成败的一环）
Pillow → WebP q82，按版面面积分级（阈值来自实测）：**面积 ≥15% 长边 1600 · ≥5% 1400 · <5% 1100**；**只缩不放**——小图硬拉大只增体积不提清晰度。
实测参考：159.5 MB / 64 张 → **5.5 MB，压缩比 29 倍**。原 PNG 冗余极大，实际结果通常远好于预估。
中间产物只有 `media_webp/`（素材在内存中压缩，不落中间目录）。**重跑会全量重压，不做按文件名复用**——不同 PPTX 的素材名都是 `image1.png` 这类包内名，复用会串素材。

### 4. 编排（工作量主体，也最容易凭感觉）

**按内容语义归章，不按页码顺序。** 同构页识别：把每页图文结构做指纹（图片数 + 面积分布 + 文本段数），结构一致的一批页天然合成一个版块（部件卡片组 / 对比线 / 概述孤页）。

**章节判据（切完自检）**：7–10 章；每章 3–6 屏；任意两章主题词不重叠；每章能一句话回答"让谁看懂什么"；一章内出现第三种结构就拆出去。
**禁止**按页码等分切章。

### 5. 构建（单文件模板）
`python scripts/build_single_html.py --work <中间产物目录> --sections <你的编排.py> --out <产物.html> [--vendor-dir <库目录>] [--no-pattern]`
脚本负责机制（占位符 `__CSS__ / __NAV__ / __BODY__ / __IMGDATA__ / __JS__ / __TOURJS__ / __VENDOR__` 替换、图片内联、定值画框比例），**编排由你的 sections 模块提供**：`page.T(页号, 前缀)` 取原文、`page.fig(key, 图注, 类名)` 出图、`specs_html([...])` 出参数表。
内置样式分两层，构建时按序拼接（机制层恒在最前，调用方 CSS 只作追加）：
- **CORE_CSS（机制层，恒注入，不可关）**：防横向溢出（`html{overflow-x:clip}`）、防图片变形（`object-fit:contain`）、防 sticky 失效、懒注入/放大/淡入依赖的类名骨架、`--vs-ar` 定值画框、禁 JS 降级与 reduced-motion。关掉它页面会坏。
- **PATTERN_CSS（版式层，默认注入，`--no-pattern` 可关）**：配色、字号、间距、栅格列数、圆角阴影、hover 动效。想彻底换皮：`--no-pattern` + 自己的 `--css`（调用方重定义 `:root` 变量即可覆盖配色）。
基础层固定在模板层注入，调用方传入的 CSS 只作追加——否则一个疏忽就会丢掉 `html{overflow-x:clip}` 这类根级约束（实测踩过，表现为整页可横向拖动）。
改完样式层必须验证**级联顺序没变**：拆分/重排后同优先级规则（如 `.ph img` 与 `.card-img img`，同为 (0,1,1)）的胜负可能反转，人读代码看不出来，用 computed style 快照前后 diff 兜底。
硬约束（都是踩过的坑）：
1. **图片数据脚本必须排在主脚本之前**，否则 `window.__IMG__` 未定义 → 全图不显示。
2. **占位符替换顺序**：含外部源码（GSAP/Three.js 等 vendor）的最后替换，避免其内部字面量被二次替换；vendor 拼接后断言不含 `</script`。
3. 类名改宽泛样式时提高选择器优先级（见坑 1）。

### 6. 静态验证（快，但不充分）
每次构建都写出 `build_report.txt`：图片引用数/内联数/缺失、未被引用、残留占位符、`cover` 计数（内容图应为 0，装饰模糊底另计）、section/卡片/图块数、vendor 体积、画框比例、正文长度。
提取全部内联 script 逐个 `node --check`；断言标签平衡。锚点齐全由第 7 段取证脚本核验——静态层看不见渲染后的 DOM id 对应关系。
**静态验证 PASS 不等于页面正确**——见坑 1。

### 7. 双视口真机取证（不可省，不只看静态检查）
playwright-core 驱动本机 chromium，**桌面（如 1440×900）+ 手机（390×844）各跑一遍**：
```js
// 横向可拖动以实际 scrollLeft 为准（overflow:hidden 下 scrollWidth 会虚报）
await page.evaluate(() => { document.scrollingElement.scrollLeft = 80; });
const pan = await page.evaluate(() => document.scrollingElement.scrollLeft); // >0 即 FAIL
```
命令：`node scripts/verify_single_html.js <产物.html> [--nowrap .nw] [--pin .cut-pin] [--shots "#ch3"]` —— 通用判据无参数即跑；项目专属判据用参数传。输出 PASS/FAIL 汇总 + 截图，退出码 0 表示全通过。
必查：① 双视口横向不可拖动 ② 控制台 0 错误 ③ 关键短语不拆行 ④ **computed style 符合预期**（不是源码里有就算）⑤ **几何级：图片无拉伸变形**（`object-fit` 为 fill/none 且盒子比 vs 素材比偏离 >2% 即 FAIL）⑥ **引用键 ↔ 内联数据差集为空**（双向；只比数量会漏掉"图 key 写错 → 该图永远空白"）⑦ **导航锚点有对应章节** ⑧ **点击放大取主体图 `img.fg`**（坑 16）⑨ 禁用 JS 内容**可读且可见**（只查文本长度会让全页透明照样 PASS）⑩ 乙档另查：`html.tour` 桌面在/手机不在、pin 区间 pinTop=0、clip/transform 随滚动变化 ⑪ 逐屏截图目检。
**结构级这七条 PASS 只说明"没崩"，不说明"对了"**——几何级（变形率）与语义级（图文配对）必须另测，见 M2。
**语义级唯一手段是人工读图**：交付前按 M2 表逐项过，脚本无法替代。
**取证陷阱见坑 9/13**：取证前 destroy 平滑滚动库，滚动后等 ≥2.5s 再截图并断言图片已加载。

---

## 机制层判据（先读这一节，再读坑清单）

下方坑清单是**实例层**（具体现象→具体修法），拦不住复发。以下七条是机制层规律——本次 4 轮用户反馈全部落在其中，逐条在交付前自检。

**M1｜禁止用"加约束"解决视觉问题——改机制，不加约束。**
为消除一个视觉症状而在更高层级加全局约束，代价必然转移到另一维度，且当时判据测不到。两个实测：为治横向拖动在 `body` 加 `overflow-x:hidden` → 毒杀全页 sticky（坑 8）；为治"图片比例不协调"把 `max-height` 改成 `height` → 横图压扁 57%（坑 12）。
**动手前先问：这条约束会不会成为别的特性的前提？** 能改机制就把约束吸收进机制（见 M6），不能则把约束限定在最小作用域（元素级 > 章节级 > 全局级）。

**M2｜判据必须分三层，缺哪层就在哪层漏缺陷。**
本次 4 次用户反馈，结构级判据**一次都没抓到**：

| 层 | 覆盖 | 可脚本断言？ | 本次漏掉的实例 |
|---|---|---|---|
| 结构级 | 数量、类名、控制台、占位符 | 是 | —（现有判据全在这层） |
| 几何级 | 盒子比例 vs 素材比例、pinTop、scrollLeft、computed style | 是 | 图片压扁 57%（只查了 cover=0，没查变形率） |
| 语义级 | 图文配对、对比标签是否成立、叙事顺序 | **否，只能人工读图** | 图文互换、"VS"两侧其实同义（坑 14） |

**语义级必须写成强制步骤，不是可选**：交付前逐卡读图核对图文配对、逐个核对"对比/VS"两侧确为对立语义。机器判据永远抓不到这两类。

**M3｜取证异常先证伪测量链路，再归因被测对象。**
三个误诊实例：截图拍到黑屏 → 实为 lazy 加载时机（坑 13）；截图落回首页 → 实为 Lenis raf 覆盖滚动（坑 9）；读图发现"磁盘内容与预期相反" → 实为多图读取时路径-内容对应记反（坑 15）。
**三步自证**：① 取证前销毁平滑滚动库（`window.__lenis?.destroy()`）；② 截图前等 ≥2.5s 并断言 `img.naturalWidth>0`；③ 读图断言用唯一前缀文件名 + 当场核对路径与内容。三步不过，不下"页面坏了"的结论。

**M4｜上一轮改动 = 下一轮回归首查清单。**
缺陷的最大来源是自己上一轮的修复动作（本次 4 次反馈中 2 次直接源于此）。改了 CSS 就先测该 CSS 影响的所有元素形态（不只测目标症状）；换了图片参数就先读图核对新配对。

**M5｜对目标元素怎么改都无效 → 问题在祖先链/运行环境。**
用消融实验改目标元素五种组合均无效时，停止改目标，查祖先的 `overflow / transform / filter / contain / will-change`——它们会创建包含块与层叠上下文，改变 sticky、fixed、100vh 的定位参照系（坑 8 即由此定位）。

**M6｜来自输入的不可控约束，用"容器机制"吸收，不做逐例硬对齐。**
素材比例不可控（实测全站 0.45~6.34，极差 14 倍）时，任何单维度硬对齐（等高/等宽/cover）都把代价转到另一维度。**范式：不对齐内容，对齐"框"**——容器 `aspect-ratio` 统一 + 主体 `object-fit:contain` + 同图模糊底填充；画框比例取组内几何平均（比例是乘性量，几何平均保证最竖/最横留白率对称）。换素材无需重调。
同源规律：**交互输入方向与视觉移动方向必须一致**，否则即劫持（坑 11）——方向一致是机制，阻尼/时长调参是补丁。

**M7｜语义标签要证伪，不默认成立。**
凡标"对比/VS""前后""优劣"的组，必须读图确认两侧确为对立语义——实测有一组是**同一现场的两张照片**被摆成对比（用户一眼看出，脚本三版都没发现）。凡卡片图文配对，必须读图确认，且结论标【推断】交用户复核。

---

## 已知坑（实例层，逐条实测）

**坑 1｜源码正则通过，浏览器渲染失效。** CSS 覆盖问题必须用浏览器 computed style 验证，查源码不算。修法：选择器升级（如 `.hero-logo.ph`）。

**坑 2｜`object-fit:cover` 是全局性灾难**，会裁掉图纸引线文字。说明书类图片一律原比例完整显示（contain 可用）；构建报告断言 cover=0 处。

**坑 3｜入场动画初始位移撑出横向可拖动区。** 修法：`html{overflow-x:hidden;overflow-x:clip}` 提到根节点。
**坑 8（v2.0 新增）｜body 上的 `overflow-x:hidden` 毒杀全页 sticky。** body 因此成为独立滚动容器而其 scrollTop 恒 0，sticky 相对它永不粘——表现为 pin 元素随文档滚走（实测 pinTop=滚出量，样式怎么改都没用）。**修法：overflow-x 约束只留在 html（clip）**，逐项消融定位时先查祖先链的 overflow。判据：pin 区间内 `pin.getBoundingClientRect().top` 应为 0。

**坑 9（v2.0 新增，v2.2 修正修法）｜平滑滚动库（Lenis 等）干扰程序化取证。** Lenis 的 raf 循环会把原生 `scrollTo`/`scrollIntoView` 的位置写回自己的插值目标，导致"滚到 X"后实际停在半途甚至被拉回顶部（截图落回首屏）。
**修法（实测 `stop()` 不够）**：取证脚本先 `window.__lenis?.destroy(); window.__lenis=null;` 再用 `window.scrollTo(0, y)`；判读以 ScrollTrigger 状态（progress/transform/clip）或 `img.naturalWidth>0` 为准，不轻信 scrollY。
**归因纪律**：滚动不到位的现象容易误诊为 `scroll-behavior:smooth`（本技能 v2.1 曾误判），先证伪是不是平滑滚动库在覆盖（M3）。

**坑 14（v2.2 新增）｜图文语义错配，任何脚本判据都抓不到。** 122 系列"竖龙骨/天地龙骨"两卡图片参数互换，六条回归全绿、三版取证都没发现，由用户一眼看出（2026-09-19）。
**判据**：卡片/图注的图文配对必须**逐张读图**核实（M2 语义层、M7）。修前先用唯一前缀文件名导出素材互证（坑 15），别只信脚本里的参数顺序。
**同源**：PPT 原稿的版面顺序也不等于语义归属——`content.json` 的图序按面积排，文本段顺序需人工对齐。

**坑 15（v2.2 新增）｜多图读取时"路径↔内容"对应极易记反。** 一次导出 4 张图逐个查看后，把 A/B 两张的内容说反了，据此差点把正确配对改错（本次实测翻车）。
**修法**：读图断言当场核对 `file_path` 与画面内容的对应；怀疑时**用全新文件名（如 `chk_<name>.png`）二次导出互证**，不复用旧目录（旧同名文件与记忆都会骗人）。

**坑 10（v2.0 新增）｜横向轨道 `width:max-content` 会撑宽文档。** 必须由 sticky 父级（自身 `overflow:hidden`）裁剪；sticky 元素自身设 overflow:hidden 不影响其粘性。

**坑 11（v2.1 新增）｜横向劫持滚轮 = 可用性事故。** pin + 横移长卷中，滚轮上下 → 内容横移，输入/视觉方向错配，用户实测"上下左右同时动、无法浏览内容"（2026-09-19 实测反馈）。**纵向 pin 擦洗可以保留（方向一致、可控）；横移劫持默认不做**——要横向内容，用 `overflow-x:auto` 让用户自己滑。评审时必须问：输入方向和视觉移动方向是否一致？

**坑 12（v2.1 新增）｜`height:固定值 + width:auto + max-width:100%` 三者共存会静默变形。** 宽度被 max-width 卡住时 height 不回缩 → 图片被压扁（实测 r=2.22 的图变形 44.8%）。为"统一行高"把 `max-height` 改成 `height` 时必然踩中。**素材比例不等是常态**，通用解法是"定值画框"：容器 `aspect-ratio` 统一 + 主体图 `object-fit:contain`（数学上保证不变形）+ 同图放大模糊做背景填充（复用同一 src，零体积增量）；画框比例取组内素材宽高比的**几何平均**（比例是乘性量，几何平均保证最竖/最横素材留白率对称，严禁硬编码 4:3）。

**坑 13（v2.1 新增，v2.2 修正归因）｜验证脚本瞬跳长距离后立即截图会拍到黑屏。** 图片全部加载正常（视口内 800ms 全就位），是截图太快；另一半原因是平滑滚动库覆盖（坑 9），与 `scroll-behavior:smooth` 无关。
修法：取证前 destroy 平滑滚动库 → `scrollTo` 定位 → 等 ≥2.5s → 断言 `img.naturalWidth>0` → 再截图。**判读时先分清"页面真坏了"还是"取证时机错了"（M3）**。

**坑 4｜参数行按空格硬切会把字段切碎**（"防火等级：A1级 ；"被切跨行）。修法：按分号切字段 + 双冒号校验才分列。

**坑 5｜"禁用 JS 则全页空白"。** 淡入动效的 `opacity:0` 只挂 `html.js-fade` 之下；乙档增强态只挂 `html.tour`（由 JS 添加），无 JS / 移动端内容照常可读。

**坑 6｜占位符 key 带不带扩展名必须统一**，否则全图 KeyError。

**坑 7｜构建报告的"残留占位符"可能是合法的**（如 `window.__IMG__={` 数据赋值 + `__IMG__ || {}` 兜底），看见计数先确认。

**坑 16（v2.4 新增，坑 12 的衍生）｜定值画框的双 img 结构会把"第一个 img"变成装饰层。**
`.vs-img` 内是 `<img class="bg">`（放大模糊底，允许 cover）+ `<img class="fg">`（主体，contain）。原脚本的点击放大用 `ph.querySelector('img')` → 打开的是模糊底图而非主体图，肉眼像"图糊了"。
**修法**：凡按"第一个 img"办事的代码（lightbox、导出、比色）一律改为优先 `img.fg`，取不到再回退。判据写进构建器与取证脚本，不靠人记。

**坑 17（v2.4 新增）｜把基础 CSS 一并外置给调用方 = 埋一个"看起来都对"的雷。**
抽通用构建器时若把样式也交给调用方传入，调用方漏传时不会报错：页面照样渲染（浏览器默认样式兜底），但 `html{overflow-x:clip}` 整层丢失 → 整页可横向拖动。**实测表现为双视口取证 FAIL，而样式表"该有的都有"**（因为基础层根本不在里面）。
**修法**：基础层固定在模板里注入，调用方传的 CSS 只作追加。判据：`getComputedStyle(document.documentElement).overflowX === 'clip'`。
**归因纪律**：这类"取证异常"要先证伪测量链路（M3）——当时用同一浏览器跑已发布的成品页作对照，成品是 `clip`、0 溢出元素，才确认问题在构建器而非取证脚本或浏览器。

---

## 环境事实与跨平台探测判据

**库获取**（乙档）：`gsap`（gsap.min.js + ScrollTrigger.min.js）与 `lenis`（lenis.min.js，UMD 全局 `Lenis`），经 npm 装入隔离 workspace 后取 `dist` 产物内联；实测合计 133.1 KB（gsap 71 + ScrollTrigger 43.5 + lenis 18.7）。
**Lenis + ScrollTrigger 对接三行**（真实滚动模式，无需 scrollerProxy）：
```js
lenis.on('scroll', ScrollTrigger.update);
gsap.ticker.add(function(t){ lenis.raf(t * 1000); });
gsap.ticker.lagSmoothing(0);
```
锚点导航改走 `lenis.scrollTo(target)`；handler 内读 `window.__lenis`（销毁后自然回退原生跳转）。
**降级门控**：所有 pin/scrub 代码包进 `gsap.matchMedia('(min-width: 961px)')` 上下文（断点与页面既有 media query 同源），返回 cleanup 移除 `html.tour`、还原 inline height、销毁 lenis。
**锚点约定**：内置漫游层找穿越段用 `[data-tour-cutaway]` 或 `#cutaway`；两者都没有时会在控制台 `warn`——**静默失效（不报错、整段不生效）比报错难查**，看到这条警告就是章节 id 没对上。

**chromium 可执行文件探测**（按 playwright 官方缓存路径枚举，取版本号最大者；`scripts/verify_single_html.js` 已内置该探测，也可用 `--chrome` 显式指定）：
- Windows: `%LOCALAPPDATA%\ms-playwright\chromium-*\chrome-win64\chrome.exe`
- Linux: `~/.cache/ms-playwright/chromium-*/chrome-linux/chrome`
- macOS: `~/Library/Caches/ms-playwright/chromium-*/chrome-mac64/chrome`（目录名按实际枚举核对）

**浏览器验证不必下载 chromium**：缓存已在则装 `playwright-core` 用 `executablePath` 直连。
**`chrome --headless --screenshot` 对 7 MB+ 页面会挂死**（实测 3 分钟无输出），走 playwright-core。

**shell 环境坑（跨平台通用）**：
- 命令含 `%` 字符（如 `%d` 格式化）在部分 Agent shell 会被安全拦截：python 代码改写 .py 文件再执行，勿用 `-c` 内联。
- 装 npm 包勿污染全局：装到独立 workspace 并显式指定 prefix；npm CLI 不在 PATH 时直接调 `node <node_dir>/node_modules/npm/bin/npm-cli.js install <pkg>`。
- 临时产物一律写系统临时目录或项目内临时目录，用完清理。

**平台专有注记（WorkBuddy 受管环境，其他平台可忽略）**：
- PowerShell 工具可能不回显 stdout：取数走"python 写结果到文件 + Read 回读"。
- Bash 可能 PATH 丢失（`dirname/head: command not found`）：改用 PowerShell。
- 受管 node 位于 `.workbuddy/binaries/node/versions/<ver>/`，npm 需拼绝对路径调用。

---

## 验收判据（交付前逐条）

| 项 | 判据 |
|---|---|
| 单文件 | 图片引用键 ↔ 内联数据**差集为空**（双向：无缺失、无冗余——只比数量会漏）；库内联无 CDN；双击可开、离线可读 |
| 文案忠实 | 正文取自提取原文；UI 导航短语术语忠于原文 |
| 图片完整 | `object-fit:cover` = 0 处（内容图；装饰模糊底另计） |
| **几何级（M2）** | 图片无拉伸变形：`object-fit` 为 `fill`/`none` 时盒子比 vs `naturalWidth/naturalHeight` 偏离 **>2% 即 FAIL**（contain/cover 下内容不变形，只提示留白偏大）；pin 区间 pinTop=0；双视口 scrollLeft=0 |
| **语义级（M2/M7）** | 逐卡读图核实图文配对；逐个核实"VS/对比"两侧确为对立语义；结论标【推断】交用户复核 |
| JS | 全部内联 script `node --check` 通过；双视口控制台 0 错误 |
| 无障碍降级 | 禁用 JS 内容仍可见；`prefers-reduced-motion` 下漫游层不启用 |
| 手机端 | 390×844 实测横向不可拖动（scrollLeft 归零）；关键短语不拆行；逐屏截图目检 |
| 桌面乙档 | `html.tour` 生效；pin 区间 pinTop=0；擦洗量随滚动变化；无横向劫持（`querySelector('.vs-track,.vs-pin')` 应为 null） |
| 可重跑 | `scripts/` 三个脚本留档，改配色/章节后秒级出结果；vendor 目录由调用方自备（本仓不内置库源码） |

## 必须交用户复核的三类事项

**总判据：凡是"判断依据不是原文"的，一律标【推断】并在交付时单独列出，绝不混进"已完成"清单。**

| 类别 | 典型事项 | 处理 |
|---|---|---|
| **语义归属** | 哪张图当穿越/剖切主体？对比页哪边是"好"的？卡片配的是不是这张图？ | 读图判断后**必须显式标注为推断**，让用户一眼可推翻；图文配对结论单独列出（坑 14） |
| **视觉观感** | LOGO、深浅色、字号、动效节奏 | 审美取舍，给带代价的选项，不自作主张 |
| **内容取舍** | 图片全留 vs 精选、是否剔除角标重复图 | 影响体积与密度，附 MB 数让用户选 |

**汇报纪律**：单列"需要你拍板的 N 件事"并给推荐次序，不散落在技术说明里。

## 交付边界

- 不承诺源文档版式像素级还原（见「定位」）。
- 不做 WebGL/3D 重做（丙档已否决，除非用户明确推翻）。
- T2 输入源（docx/pdf/图片目录）首次执行前向用户说明"未实测"，跑通后回填。
